# cloudbuild.yaml — BandAid (Google Cloud Build)
# Builds and pushes: gcr.io/bandaid-489018/bandaid-app:latest

steps:
  - name: "gcr.io/cloud-builders/docker"
    id: "Build container image"
    args:
      - "build"
      - "-t"
      - "us-central1-docker.pkg.dev/bandaid-489018/bandaid-repo/bandaid-app:latest"
      - "."

images:
  - "us-central1-docker.pkg.dev/bandaid-489018/bandaid-repo/bandaid-app:latest"
